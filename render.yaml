services:
  - type: web
    name: alexa-gemini-skill
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: GEMINI_API_KEY
        value: ${GEMINI_API_KEY}
      - key: PORT
        value: 3000
